name: Matrix.sc
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.17
  firebase_storage: ^12.4.1
  image_picker: ^1.1.2
  firebase_core: ^3.10.1  # The following adds the Cupertino Icons font to your application.
 # Use with the CupertinoIcons class for iOS style icons.
  cloud_firestore: ^5.6.2
  firebase_auth: ^5.4.1
  flutter_easyloading: ^3.0.5
  random_string: ^2.3.1
  cached_network_image: ^3.4.1
  card_loading: ^0.3.2
  # flutter_product_card: ^0.0.7
  # image_card: ^0.0.4
  google_fonts: ^6.2.1
  provider: ^6.1.2
  # shared_preferences: ^2.3.3
  # flutter_admin_scaffold: ^1.3.0
  file_picker: ^8.1.7
  # flutter_gen: ^5.8.0
  # timer_count_down: ^2.2.2
  animations: ^2.0.11
  flutter_launcher_icons: ^0.14.3
  # flutter_plugin_android_lifecycle: ^2.0.3
  # flutter_native_splash: ^2.3.3
  connectivity_plus: ^6.1.3
  

dev_dependencies:
  flutter_test:
    sdk: flutter


#   flutter_lints: ^5.0.0
# flutter_native_splash:
#   color: "#ffffff"  # Background color
#   image: assets/icon/icon.png  # Your splash image
#   android: true
#   ios: true
#   fullscreen: true
  

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 16
  # android min sdk min:16, default 16/
  adaptive_icon_background: "#ffffff"  #color or image for the backround
  adaptive_icon_foreground: "assets/icon/icon.png"
  
  
flutter:

  uses-material-design: true
  assets:
    - assets/icons/
    - assets/images/
    - assets/icon/
    - assets/svgions/
  fonts:
    - family: Brand-bold
      fonts:
        - asset: assets/fonts/GemunuLibre-Bold.ttf

    - family: Semi-bold
      fonts:
        - asset: assets/fonts/GemunuLibre-SemiBold.ttf


